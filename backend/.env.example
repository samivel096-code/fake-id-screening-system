# Optional comma-separated additional browser origins. FRONTEND_URL is required
# in production and is always added to the allowed origins.
CORS_ORIGINS=

# Deployed Vercel frontend URL in production.
FRONTEND_URL=https://YOUR-ACTUAL-VERCEL-DOMAIN.vercel.app

# Required for signing authentication tokens.
JWT_SECRET=